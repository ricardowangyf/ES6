var obj = {
    name: "abc",
    org: "yellow"
}

function format(str) {
    return str
}

console.log(format(obj))
    // =>  "'name+org' = 'abc,yellow'"
    // => ['name', 'org']

// => ['abc', 'yellow']

// => abc+yellow

//key value